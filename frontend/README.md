# Frontend

Angular SSR frontend scaffold for the portfolio.

## Commands

```bash
npm install
npm start
npm run format:check
npm run lint
npm test
npm run build
```

Run `npm run serve:ssr` after `npm run build` to start the built SSR server.
